services:
  web:
    image: ghostvoid:latest
    ports:
      - "8000:8200"
      - "5678:5678"
    volumes:
      - .:/app
    command: ["python", "-m", "debugpy", "--listen", "0.0.0.0:5678", "--wait-for-client", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8200"]
